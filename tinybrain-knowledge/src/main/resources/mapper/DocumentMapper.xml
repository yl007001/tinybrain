<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tinybrain.knowledge.mapper.DocumentMapper">

    <!-- 通用查询列 -->
    <sql id="baseColumns">
        id, title, summary, content, content_type, status,
        tags, user_id, deleted, create_time, update_time
    </sql>

    <!-- 分页 + 多条件查询用户文档 -->
    <select id="selectUserDocuments" resultType="com.tinybrain.knowledge.entity.Document">
        SELECT <include refid="baseColumns"/>
        FROM kb_document
        WHERE deleted = 0
        AND user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%')
                 OR summary LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="tags != null and tags.size() > 0">
            AND (
            <foreach collection="tags" item="tag" separator=" OR ">
                JSON_CONTAINS(tags, CONCAT('"', #{tag}, '"'))
            </foreach>
            )
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY update_time DESC
    </select>
</mapper>
